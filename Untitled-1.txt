      {/* Distribución por categoría (pie simple existente) */}
      <View className="bg-white rounded-2xl p-4 shadow-sm border border-gray-100">
        <Text className="text-gray-600 text-xs mb-3">Ventas por categoría</Text>
        <View className="items-center">
          <PieChart
            data={pieData}
            donut
            radius={90}
            innerRadius={60}
            innerCircleColor="#FFFFFF"
            sectionAutoFocus
            showText
            textColor="#000"
            textSize={12}
            centerLabelComponent={() => (
              <View className="items-center">
                <Text className="text-black font-semibold text-xl">40%</Text>
                <Text className="text-black">Top categoría</Text>
              </View>
            )}
          />
        </View>
      </View>

      {/* Ventas por día */}
      <SalesBar data={ventasPorDia} />

      {/* Otros componentes ya existentes */}